# Pull request #1 — Sitio estático de vigiabref.com, generado desde los informes del pipeline

| | |
|---|---|
| **Estado** | closed · fusionado |
| **Autor** | @vigiabref |
| **Creado** | 2026-08-03T05:58:13Z |
| **Fusionado** | 2026-08-03T06:11:35Z |
| **Commit de fusión** | `55ee4ced8a3f5aaed3e72ec206466af1a60ee9c5` |
| **Rama** | `claude/sitio-inicial` → `main` |
| **URL original** | https://github.com/vigiabref/portafolio/pull/1 |
| **Comentarios archivados** | 0 |

## Descripción

Implementación del sitio a partir de la maqueta. Tres vistas —portada, proyecto, informes— y **una página por informe publicado**, con URLs reales en lugar del estado en cliente de la maqueta.

Estático puro: **cero JavaScript, cero recursos de terceros, cero dependencias en tiempo de ejecución.**

## 1 · Los informes no se escriben a mano

`sitio/lector.py` lee `reports/AAAA/AAAA-MM-DD.md` del pipeline. De ahí salen la sección de informes —el más reciente en `/informes/`, y el archivo navegable por fecha— y las cifras de portada y de proyecto.

La maqueta traía esas cifras escritas: «76%», «7.464 indicadores», «68 de 90 familias», Mirai con 1.284. Para cuando se implementó el sitio ya eran otras. **Una cifra fijada en el código envejece en silencio, que es la peor forma de envejecer: sigue pareciendo una medición.**

Y lo que el informe no publica, el sitio tampoco: una magnitud suprimida sale como guion **con su motivo**, no como cero.

## 2 · Tipografía propia, y la promesa del pie ejecutada

Inter viaja en `estatico/fuentes/` con su licencia SIL OFL. El pie dice que no hay analítica, formularios ni recursos de terceros — y `tests/test_sitio.py` lo comprueba. La maqueta incumplía esa misma línea cargando Inter desde Google Fonts.

## 3 · Reconstrucción automática

`desplegar.yml` escucha `repository_dispatch: informe-publicado`, que emite el workflow diario del pipeline ([`claude/disparo-sitio`](https://github.com/vigiabref/threat-intel-pipeline/tree/claude/disparo-sitio), pendiente de fusionar).

## 4 · Móvil

El archivo pasa de columna fija de 216 px a tira desplazable con objetivos táctiles de 44 px. Las tablas se convierten en **fichas apiladas** por debajo de 700 px, con el rótulo de columna en `data-columna`. El marcado sigue siendo `<table>`: un lector de pantalla conserva la relación celda-cabecera.

## 5 · Despliegue

GitHub Pages sobre `vigiabref.com`. Acciones fijadas por hash, `permissions: {}` global, y el despliegue cuelga de la construcción: si falla, no se publica nada.

---

## Revisión independiente: 2 bloqueantes, 4 relevantes, 4 menores

Acta íntegra en [`docs/revisiones/sitio--pasada-1.md`](docs/revisiones/sitio--pasada-1.md). Los dos bloqueantes eran el mismo defecto por sus dos caras:

**1.** Los informes reales declaran «No se publica el panorama de familias» **y aun así** imprimen «denominador: 88 familias observadas» en el reparto de motivos de su nota metodológica. La expresión regular buscaba en todo el texto y encontraba **esa**. El sitio publicaba **75%** como medición destacada de portada y, en la página de informes, «88 familias observadas» dos bloques por encima de la laguna que lo desmentía. La misma página contradiciéndose.

**2.** La fixture del caso «sin familias» estaba **editada a mano**, mientras la del informe real —byte a byte idéntica— recorría la rama defectuosa en verde. El instrumento estaba calibrado sobre un caso construido para pasar.

Corregido: **la supresión que declara el informe manda sobre cualquier cifra que aparezca en él.** El productor decide qué publica; el consumidor no puede reconstruirlo de las migas. Los tests que lo vigilan corren sobre los informes reales sin retocar, y la fixture construida cubre el caso sano —que hoy el pipeline no produce— diciendo por qué.

| Mutación | Resultado |
|---|---|
| Quitar la comprobación de supresión | muere en 3 tests |
| Añadir una cifra a mano *(la que sobrevivió al revisor)* | muere en `test_las_cifras_escritas_a_mano...` |
| CDN externa, `<script>`, `@import` a Google Fonts | mueren, ya en la primera pasada |

**37 tests en verde**, `ruff` limpio.

## Dos condiciones para lanzar

**La cifra de portada no se puede publicar hoy.** Con el panorama suprimido en los dos informes, el titular queda en guion. Está implementado el patrón que usa la especificación del pipeline —heredarla del informe más reciente que sí la publique, con su fecha declarada— pero ninguno lo hace: ThreatFox lleva desde el bloque 2 en `parcial` por la cobertura de `reference`.

**Los enlaces «puedes comprobarlo» dan 404** mientras `threat-intel-pipeline` sea privado. Queda escrito en el README como requisito, no como nota al pie.

---
_Generated by [Claude Code](https://claude.ai/code/session_01K5fGEuoXwWjSTa8SbFNUyC)_

## Hilo — 0 entradas, en orden cronológico

*Cada entrada va precedida de una regla horizontal y de su línea de atribución en negrita. Los encabezados que aparezcan dentro de una entrada pertenecen al texto original y se conservan tal cual.*

*El pull request no tiene comentarios, revisiones ni comentarios en línea.*
